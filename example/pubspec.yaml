name: example
description: Demonstrates how to use the bmprogresshud plugin.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  bmprogresshud:
    path: ../

flutter:

  uses-material-design: true

